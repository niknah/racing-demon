{
  "type": "module",
  "scripts": {
    "dev": "npx vite dev",
    "build": "npx vite build",
    "build_cardsJS": "cd cardsJS; npm install; npx gulp dist",
    "all": " npm run build_cardsJS; npm run build;"
  },
  "dependencies": {
    "nanoevents": "^9.0.0",
    "vite": "^5.2.2",
    "vite-plugin-static-copy": "^1.0.1"
  }
}
